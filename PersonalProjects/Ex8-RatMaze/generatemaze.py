#Prim's algorithm
import numpy as np

size = int(input("Choose the size of a maze: "))
maze = np.full((size,size), "◼")
print(maze)

init_x = 0
init_y = 0

wall_x = []
wall_y = []

maze_x = []
maze_y = []

def addWalls(x, y, maze):
    if x > 1 and x < len(maze)


def genMaze(curr_x, curr_y, maze_x, maze_y, maze, wall_x, wall_y ):
    maze_x = append.curr_x
    maze_y = append.curr_y

    addWalls(curr_x, curr_y)
    while 
